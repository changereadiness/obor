(async()=>{const el=document.querySelector('#signal-grid');if(!el)return;try{const r=await fetch('/data/signals.json');const signals=await r.json();const live=signals.filter(s=>s.status!=='suppressed').sort((a,b)=>b.relevance_score-a.relevance_score).slice(0,6);el.innerHTML=live.map(s=>`<article class="signal-card"><div class="meta"><span class="tag">${s.opportunity_or_risk}</span><span class="score">OBOR ${s.relevance_score}/100</span></div><h3><a href="/signals/${s.slug}/" style="color:inherit;text-decoration:none">${s.title}</a></h3><p>${s.summary}</p><p class="why"><strong>Why it matters</strong>${s.canadian_relevance}</p></article>`).join('')}catch(e){el.innerHTML='<p>Today’s intelligence is temporarily unavailable.</p>'}})();
